Sprint #49: Cron Telemetry Rollups, Reactive Stats UI, & WCAG Accessibility

1. Cloudflare Cron Trigger Nightly Metrics Rollup:
   - Updated `wrangler.jsonc` to include `triggers = { crons = ["0 0 * * *"] }`.
   - Updated `src/worker.js` with `scheduled(event, env, ctx)` handler to execute at midnight UTC.
   - The handler aggregates KV metrics and inserts a summary row into D1 `DailyTelemetrySummaries`.

2. Instant Stats Panel UI Reactivity:
   - Added `gamesWon` state to `src/hooks/useGameEngine.js` and ensured it persists in localStorage.
   - Updated `src/App.jsx` to pass `gamesWon` down to the StatsPanel.
   - Updated `src/components/StatsPanel.jsx` to wrap stats in `motion.span` elements with a `key` property to trigger animated pulsing on increment.

3. Retro Terminal ARIA & Keyboard Accessibility:
   - Added `aria-label` logic for keys in `src/components/Keyboard.jsx`.
   - Added an `aria-live="polite"` element for visual-only screen reader updates in `src/components/GameBoard.jsx`.
   - Updated `src/index.css` to add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-green-400` styling.
