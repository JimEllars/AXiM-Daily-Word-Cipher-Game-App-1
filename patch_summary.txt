Sprint 47 - Edge Rate Limiting, Telemetry Validation & SSO Token Lifecycle

1. Cloudflare Worker Rate Limiting (`src/worker.js`):
   - Validated that the in-memory token-bucket limits `POST /api/telemetry` to 60 requests/min and `POST /api/user/sync` to 20 requests/min, returning HTTP 429 correctly.

2. Telemetry Payload Schema Validation (`src/worker.js`):
   - Enforced 2KB payload size limit by checking content-length and raw text length.
   - Prevented D1 database exhaustion from malformed blobs safely by rejecting invalid/bloated requests with HTTP 400.

3. Client-Side 401 Auto-Recovery (`src/hooks/useGameEngine.js`, `src/components/LoginButton.jsx`):
   - Caught HTTP 401 Unauthorized responses inside `useGameEngine.js`.
   - Restored `handleAuthError()` upon 401 which clears SSO tokens, triggers `axim_sso_expired` to revert the UI, and dispatches an `axim_toast` showing `[ SESSION EXPIRED: PLEASE RE-LOGIN ]`.
   - Preserved active puzzle board state and logic seamlessly avoiding page reloads or interruption.
