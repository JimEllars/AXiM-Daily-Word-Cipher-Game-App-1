Patch Summary - Sprint #34:

- Task 1: Replaced the standalone game header in `src/components/Header.jsx` with a global AXiM header incorporating navigation links and a unified aesthetic mimicking the parent site. Maintained sticky positioning and viewport scaling.
- Task 2: Renamed `WalletButton.jsx` to `LoginButton.jsx` and updated it to render a dropdown modal with "Login with Email" and "Connect Web3 Wallet" options. It now accurately displays "Hi, {username}" or truncated wallet address on successful authentication. Integrated this new component seamlessly into the top-right of the `Header` component.
- Task 3: Restored the logic for guess distribution calculations in `src/hooks/useGameEngine.js`. Created a new `GuessDistribution.jsx` component and positioned it below the game board in `src/App.jsx`, ensuring it reads and displays guess metrics minimally at the bottom of the viewport.
