Sprint #53 - Top-Left Clickable Brand Logo, New Tab Isolation, Navigation Telemetry, & Responsive Header Balancing

Modifications:
- src/hooks/useTelemetry.js: Added `keepalive: true` to the fetch request in `trackEvent` to ensure telemetry reliably fires when a user clicks the logo to open a new tab/window without being dropped due to page unloads or quick navigations.
- src/components/Header.jsx:
  - Restructured the flexbox layout to support wrapping on mobile screens (<380px) and display the `edgeHealth` (passed via props) in the center.
  - Replaced the logo with the updated AXiM Development brand logo and configured it as an anchor linking to the games portal (`target="_blank"`, `rel="noopener noreferrer"`).
  - Wired `useTelemetry` to trigger `NAVIGATED_TO_AXIM_GAMES` immediately upon clicking the logo link.
- src/App.jsx: Passed `edgeHealth` to the `<Header />` component and removed the duplicated/old edge health display at the bottom of the layout grid to consolidate header UI components.
